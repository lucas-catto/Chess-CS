
@model List<Players>

@{
    ViewData["Title"] = "Home Page";

    var years = (List<Years>)ViewData["years"];
}

<head>
     <link rel="stylesheet" href="~/css/Index/index.css">
</head>

<section id="s1">
    <p id="all">All</p>
    @foreach (var year in years) {
        <p style="background-color:@year.Color;">@year.Year's</p>
    }
</section>

<section id="s2">
    @foreach (var player in Model) {

        @if (player.Player_Id % 2 != 0) {

            <div class="card black" onclick="window.location='/Home/Details/@player.Player_Id'">
                
                <img src="~/@player.Image">

                <h2>@player.Name</h2>

                <div class="dates">
                    <p>@player.YearBorn</p>
                    <p>-</p>
                    <p>@player.YearDied</p>
                </div>
            </div>
        }
        else {
            <div class="card white" onclick="window.location='/Home/Details/@player.Player_Id'">
                
                <img src="~/@player.Image">

                <h2>@player.Name</h2>
                
                <div class="dates">
                    <p>@player.YearBorn</p>
                    <p>-</p>
                    <p>@player.YearDied</p>
                </div>
            </div>
        }

        @* foreach (var year in player.Activity) {
            <p></p>
        } *@
    }
</section>